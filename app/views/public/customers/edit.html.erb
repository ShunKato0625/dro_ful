<div class="container py-5">
  <div class="row my-5">
    <div class="col-5 text-center">
      <h2 class="heading edit-profile" data-en="EditProfile">会員情報編集</h2>
    </div>
  </div>

  <div class = "row error-massage">
    <div id="error" class="ml-2"><%= render 'layouts/errors', obj: @customer %></div>
  </div>

  <%= form_with model:@customer, url: customer_path(@customer.id), method: :patch do |f| %>
    <div class="form-group row">
      <%= f.label :プロフィール画像, class: "col-md-3 col-12" %>
      <%= f.file_field :profile_image, class:"form-control-file col-md-6 col-10 offset-1" %>
    </div>

    <div class="form-group row">
      <%= f.label :nick_name, 'ニックネーム', class: "col-md-3 col-12" %>
      <%= f.text_field :nick_name, autocomplete: "nick_name", class: "form-control col-md-6 col-10 offset-1" %>
    </div>

    <div class="form-group row">
      <%= f.label :email, 'メールアドレス', class: "col-md-3 col-12" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control col-md-6 col-10 offset-1" %>
    </div>

    <div class="form-group row">
      <%= f.label :introduction, '自己紹介', class: "col-12" %>
      <%= f.text_area :introduction, autofocus: true, autocomplete: "introduction", class: "form-control col-md-6 col-10 offset-1" %>
    </div>

    <div class="actions mt-5">
      <%= f.submit "変更を保存", class: "btn btn-success col-4 offset-2" %>

      <!-- Button trigger modal -->
      <button type="button" class="btn btn-danger col-3 offset-1" data-toggle="modal" data-target="#exampleModalCenter">
        退会する
      </button>

    </div>
  <% end %>
</div>

  <!-- Modal -->
  <div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalCenterTitle"><b>本当に退会しますか？</b></h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          退会すると、会員登録情報や<br>
          これまでの投稿履歴が閲覧できなくなります。<br>
          退会する場合は、「退会する」をクリックしてください。
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">キャンセル</button>
          <%= link_to '退会する', customer_withdraw_path(@customer.id), method: :patch, class: "btn btn-danger", type:"button" %>
        </div>
      </div>
    </div>
  </div>